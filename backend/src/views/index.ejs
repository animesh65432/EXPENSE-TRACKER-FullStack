<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Report</title>
    <style>
        html,
        body {
            padding: 0;
            margin: 0;
            height: 100%;
            background-color: white;
            color: black;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        * {
            box-sizing: border-box;
        }

        #main-container {
            padding: 50px;
            width: 90%;
            margin: auto;
        }

        .Expense-Title {
            font-size: 28px;
            font-weight: bold;
            text-transform: uppercase;
            padding: 15px 0;
            border-bottom: 2px solid black;
            text-align: center;
            letter-spacing: 2px;
            color: black;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
            border: 1px solid black;
            color: black;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid black;
        }

        th {
            background-color: black;
            font-weight: bold;
            text-transform: uppercase;
        }

        tr:nth-child(even) td {
            background-color: #f2f2f2;
            color: black;
        }

        tr:nth-child(odd) td {
            background-color: white;
            color: black;
        }

        #ExpenseTotalamount {
            font-size: 22px;
            font-weight: bold;
            border-top: 2px solid black;
            padding: 15px;
            margin-top: 30px;
            text-align: center;
            display: flex;
            justify-content: space-between;
            background-color: black;
            color: black;
        }
    </style>
</head>

<body>
    <main id="main-container">
        <div class="Expense-Title">Your Expenses</div>
        <table>
            <thead>
                <tr>
                    <th>Expense Name</th>
                    <th>Description</th>
                    <th>Category</th>
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>
                <% expenses.forEach(expense=> { %>
                    <tr>
                        <td>
                            <%= expense.ExpensesName %>
                        </td>
                        <td>
                            <%= expense.description %>
                        </td>
                        <td>
                            <%= expense.Category %>
                        </td>
                        <td>
                            <%= expense.Expenseamount %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
        <div id="ExpenseTotalamount">
            <span>Total Expense Amount:</span>
            <span>
                <%= totalexpenses %>
            </span>
        </div>
    </main>
</body>

</html>